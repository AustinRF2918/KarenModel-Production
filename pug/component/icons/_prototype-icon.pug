//Refactor later for usability: Take the svg data and pack it into a unique mixin and then
//make it part of a building block to make modular icons.

mixin generalIcon(className, titleName, svgInfo) 
  a(href="#", class=["general-icon",  className], title=titleName)
    svg(viewBox="0 0 512 512")
        path(d=svgInfo)